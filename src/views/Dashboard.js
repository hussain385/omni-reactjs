import '../App.css';
import { useRef } from 'react';
import SideBar from '../components/SideBar/SideBar';

function Dashboard() {
    return (
        <SideBar>
            <h1>Dashboard</h1>
        </SideBar>
    )
}

export default Dashboard